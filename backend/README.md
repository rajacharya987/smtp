# MailGate backend

Python package: FastAPI app, CLI, Postfix map generator, DNS checks, worker.

```bash
pip install -e ".[dev]"
pytest
python -m mailgate --help
```

See the repository root README and `docs/DEVELOPMENT.md`.
